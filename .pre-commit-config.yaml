repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.8.4
    hooks:
      - id: ruff
        args: [--fix]

# Note: mypy is run in CI only, not in pre-commit
# because it requires all project dependencies to be installed
